export function ContactPage() {
  return (
    <section className="space-y-4">
      <p className="text-sm font-medium uppercase tracking-[0.2em] text-primary">Contact shell</p>
      <h1 className="text-3xl font-semibold tracking-tight">Contact details will come from company config.</h1>
      <p className="max-w-3xl text-muted-foreground">
        The route exists now so future company settings, map placeholders, and lead capture UI have a stable place in the app
        structure.
      </p>
    </section>
  )
}
